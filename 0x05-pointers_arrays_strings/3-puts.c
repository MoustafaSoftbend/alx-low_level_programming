#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _puts - prints a string, followed by a new line, to stdout
 * @str: pointer to string to be printed
 *
 * Return: void
 */
void _puts(char *str)
{
	puts(str);
}
